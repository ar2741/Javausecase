import React from "react";
import { 
  FaTachometerAlt, 
  FaUsers, 
  FaLock, 
  FaFileAlt, 
  FaCog, 
  FaUserCircle, 
  FaQuestionCircle 
} from "react-icons/fa";

function Sidebar() {
  // Simple click handler for menu items
  const handleClick = (menuItem) => {
    console.log(`${menuItem} clicked`);
    // Add your button functionality here
    // You can replace this with state management or other logic
  };

  return (
    <div className="sidebar">
      <img src="/logo.png" alt="Standard Chartered" className="logo" />
      
      <ul>
        <li onClick={() => handleClick('Dashboard')}>
          <FaTachometerAlt /> Dashboard
        </li>
        <li onClick={() => handleClick('User Management')}>
          <FaUsers /> User Management
        </li>
        <li onClick={() => handleClick('AD Group Access')}>
          <FaLock /> AD Group Access
        </li>
        <li onClick={() => handleClick('Report Access')}>
          <FaFileAlt /> Report Access
        </li>
        <li onClick={() => handleClick('Settings')}>
          <FaCog /> Settings
        </li>
      </ul>
      
      <div className="sidebar-bottom">
        <li onClick={() => handleClick('Account')}>
          <FaUserCircle /> Account
        </li>
        <li onClick={() => handleClick('Help')}>
          <FaQuestionCircle /> Help
        </li>
      </div>
    </div>
  );
}

export default Sidebar;
