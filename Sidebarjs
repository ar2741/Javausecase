import React from "react";
import { 
  FaTachometerAlt, 
  FaUsers, 
  FaLock, 
  FaFileAlt, 
  FaCog, 
  FaUserCircle, 
  FaQuestionCircle 
} from "react-icons/fa";

function Sidebar() {
  // Click handler for menu items
  const handleButtonClick = (buttonName) => {
    console.log(`${buttonName} clicked`); // Replace with your desired action
    // You can add your specific logic for each button here
  };

  return (
    <div className="sidebar">
      <img src="/logo.png" alt="Standard Chartered" className="logo" />
      
      <ul>
        <li onClick={() => handleButtonClick('Dashboard')}>
          <FaTachometerAlt /> Dashboard
        </li>
        <li onClick={() => handleButtonClick('User Management')}>
          <FaUsers /> User Management
        </li>
        <li onClick={() => handleButtonClick('AD Group Access')}>
          <FaLock /> AD Group Access
        </li>
        <li onClick={() => handleButtonClick('Report Access')}>
          <FaFileAlt /> Report Access
        </li>
        <li onClick={() => handleButtonClick('Settings')}>
          <FaCog /> Settings
        </li>
      </ul>
      
      <div className="sidebar-bottom">
        <li onClick={() => handleButtonClick('Account')}>
          <FaUserCircle /> Account
        </li>
        <li onClick={() => handleButtonClick('Help')}>
          <FaQuestionCircle /> Help
        </li>
      </div>
    </div>
  );
}

export default Sidebar;
